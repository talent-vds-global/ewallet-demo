server:
  port: 8081

spring:
  application:
    name: ewallet-business-customer-mobileapp

clients:
  order:
    base-url: ${CLIENTS_ORDER_BASE_URL:http://ewallet-payment-order:8082}

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      probes:
        enabled: true
